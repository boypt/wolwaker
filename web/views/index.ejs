<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>
    <ul>
      <% _.forIn(gOnlineDict, function(value, gkey){ 
          _.forEach(value.pairs, function(ps) { %>
        <li><%= value.remote[0] %> - <%= ps[1] %>|<%= ps[0] %> <a href="/callwake?key=<%=gkey%>&mac=<%=ps[0]%>" >WAKE</a></li>
        <% })
      }) %>
    </ul>
  </body>
</html>
